# v3.1 Spec Sign-off Attestation

**Spec version:** docs/superpowers/specs/v3.1-stress-subtest-enum.md
**Approval mode:** HMAC-attested via `/spec-approve` (canonical-payload contract from `src/audit_trail/hmac_verify.py`)
**Operator:** saehoon0501
**Sign-off timestamp:** 2026-05-20T14:01:56.496646+00:00
**Approved via:** `python -m src.spec_approve.cli 3.1`

## Scope of approval

Approves the v3.1 Phase 0 spec revision: canonical §2.6 stress sub-test enum (5 sub-tests including STRESS_GENERIC_CLAIM_INVERSION_FAILED residual category with search-grounding). Closes the spot/IV-divergence-class freeform-stress loophole that produced the Phase 5a Wave 2 A1-tight TRIM vs A7-tight HOLD divergence. Operator-clarified scope: fresh external data pulls allowed for STRESS_GENERIC; pm-supervisor MCP grants to be expanded in Phase B (WebFetch + edgar + market_data + yfinance + fundamentals). CAF-A architectural inversion (specialists-pull-data) verdict: significant but mitigated via evidence_index caching + HG-1 + audit-trail. Phase B implementation may proceed.

## Effects of sign-off

1. **v3.1 frozen** — immutable post-attestation. Subsequent revisions tracked as a new version with explicit change-log per Section 8 PB#1.
2. **Implementation may proceed** per the v3.1 spec.
3. **Launch gates active** per v3.1 Section 7 (or successor).

## Caveats

- HMAC signature below is computed over a canonical JSON payload of
  (version, spec_path, timestamp, operator) using `AUDIT_HMAC_KEY`.
  Verify by recomputing with the same canonical-payload contract.
- For replay verification, see `src/audit_trail/hmac_verify.py`.

## HMAC signature

```
f3f87160cdcfc15b29bace5fb7d360cab9a510404f379a12c6a35644116d4aac
```

---

**Spec-approve attestation captured.** v3.1 frozen.
